qdrant-client>=1.10.0
sentence-transformers>=3.0.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
polars>=0.20.0
streamlit>=1.32.0
redis>=5.0.0
prometheus-client>=0.21.0
pytest>=8.0.0
mypy>=1.8.0
flake8>=7.0.0
types-requests>=2.32.0
fastapi>=0.100.0
uvicorn>=0.22.0
python-dotenv>=1.0.0

# Multilingual NLP for Swiss retail chatbot
langdetect>=1.0.9

# Knowledge Graph (Neo4j)
neo4j>=5.0.0

# Dynamic pricing (MARL + RAG)
pandas>=2.0.0
scipy>=1.10.0
plotly>=5.0.0

# Price forecasting (optional Prophet for seasonal agent)
prophet>=1.1.0

# GENAI-HACKATHON: Prompt engineering & validation
jinja2>=3.1.0
pydantic>=2.0.0
pytest-asyncio>=0.23.0

# Geospatial mapping & logistics (PostGIS, Mapbox/OSRM, async DB)
asyncpg>=0.29.0
aiohttp>=3.9.0

# LiveMap privacy helpers
cryptography>=42.0.0

# Production external providers (Qdrant Cloud + HF + Whisper + Replicate + Groq + ElevenLabs)
openai>=1.0.0
replicate>=0.25.0
groq>=0.4.0
requests>=2.28.0

# Voice production stack (Swiss German STT + on-device TTS)
openai-whisper>=20231117
piper-tts>=1.2.0
websockets>=12.0

# Swiss cultural backend (TWINT, PostFinance, QR)
httpx>=0.25.0
qrcode[pil]>=7.0

# Apertus-70B + HF datasets
datasets>=2.18.0
huggingface-hub>=0.22.0
transformers>=4.40.0
