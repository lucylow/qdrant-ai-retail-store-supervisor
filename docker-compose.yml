version: "3.9"

services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app

  web:
    build: ./web
    ports:
      - "8080:8080"
    depends_on:
      - server

volumes:
  qdrant-data: {}
